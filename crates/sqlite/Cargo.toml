[package]
name = "spin-sqlite"
version = { workspace = true }
authors = { workspace = true }
edition = { workspace = true }

[dependencies]
serde = { workspace = true }
spin-factor-sqlite = { path = "../factor-sqlite" }
spin-factors = { path = "../factors" }
spin-sqlite-inproc = { path = "../sqlite-inproc" }
spin-sqlite-libsql = { path = "../sqlite-libsql" }
# The Turso local-first sync backend (`type = "turso"`) is built in on this branch.
# NOTE: the underlying Turso engine's offline sync is beta (no durability guarantees
# yet) and pulls a large dependency tree.
spin-sqlite-turso = { path = "../sqlite-turso" }
toml = { workspace = true }
